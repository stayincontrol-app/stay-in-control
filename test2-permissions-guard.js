(()=>{'use strict';const AUTH='ap207-auth-profile-v1';function read(){try{return JSON.parse(localStorage.getItem(AUTH)||'null')}catch{return null}}function ids(a){const p=a?.profile;const raw=Array.isArray(p?.propertyIds)?p.propertyIds:Array.isArray(p?.property_ids)?p.property_ids:Array.isArray(a?.propertyIds)?a.propertyIds:[];return new Set(raw.map(String))}function install(){const api=window.AP207Access;if(!api||api.__t2Hardened)return false;const original=api.visibleProperties;api.visibleProperties=function(user,properties){const a=read(),p=a?.profile,list=Array.isArray(properties)?properties:[];if(!p||p.active===false)return[];if(p.role==='super_admin')return list.slice();if(!['admin','owner'].includes(p.role))return[];const allowed=ids(a);return list.filter(x=>x?.id!=null&&allowed.has(String(x.id)))};api.assertPropertyAccess=function(user,property){if(!property||!api.visibleProperties(user,[property]).length)throw new Error('Acesso negado a esta propriedade.');return true};api.authorize=function(user,property,permission){api.assertPermission(user,permission);api.assertPropertyAccess(user,property);return true};api.perform=function(user,property,permission,operation){api.authorize(user,property,permission);return operation()};Object.defineProperty(api,'__t2Hardened',{value:true});return true}function scrubSetPassword(){const card=document.querySelector('.ap207-auth-card'),note=card?.querySelector('.ap207-success-note strong');if(!note)return;const text=String(note.textContent||'').trim();note.replaceChildren(document.createTextNode(text||'E-mail confirmado'))}function boot(){let tries=0;const t=setInterval(()=>{tries++;install();scrubSetPassword();if(tries>80)clearInterval(t)},250);new MutationObserver(scrubSetPassword).observe(document.body,{subtree:true,childList:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();