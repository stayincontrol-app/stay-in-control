(()=>{'use strict';
const RES='ap207-dashboard-reservations-v1',EXP='ap207-dashboard-expenses-v1',PROPS='ap207-dashboard-properties-v1';
const money=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'});
function read(k,f){try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch{return f}}
function props(){return read(PROPS,{properties:[]}).properties||[]}
function is207(p){return `${p?.id||''} ${p?.name||''} ${p?.unit||''}`.toLowerCase().includes('207')}
function records(key,p,field){const scoped=read(`${key}:${p.id}`,null);if(Array.isArray(scoped?.[field]))return scoped[field];if(is207(p)){const legacy=read(key,null);if(Array.isArray(legacy?.[field]))return legacy[field]}return[]}
function prefix(y,m){return `${y}-${String(m).padStart(2,'0')}`}
function baseCalc(p,y,m){const pref=prefix(y,m),rs=records(RES,p,'reservations').filter(r=>r.status!=='Cancelada'&&String(r.checkIn||'').startsWith(pref)),es=records(EXP,p,'expenses').filter(e=>String(e.date||'').startsWith(pref));let gross=0,cleaning=0,commission=0,nights=0;for(const r of rs){const g=Number(r.gross)||0,c=Number(r.cleaningFee??r.cleaning)||0,rate=Number(r.commissionRate)||0;gross+=g;cleaning+=c;commission+=(g-c)*rate/100;const a=new Date(r.checkIn),b=new Date(r.checkOut);if(!isNaN(a)&&!isNaN(b))nights+=Math.max(0,(b-a)/86400000)}const expenses=es.reduce((s,e)=>s+(Number(e.value)||0),0);return{reservations:rs.length,nights,gross,cleaning,commission,expenses,recurring:0,payout:gross-cleaning-commission-expenses}}
function today(){const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function dateStr(y,m,d){return`${y}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`}
function dim(y,m){return new Date(y,m,0).getDate()}
function occurrences(r,y,m){if(!r?.active||!r.starts_on)return[];const [sy,sm,sd]=String(r.starts_on).slice(0,10).split('-').map(Number);if(!sy||!sm||!sd)return[];const start=dateStr(sy,sm,sd),last=dim(y,m),due=Math.min(last,Math.max(1,Number(r.due_day)||sd)),diff=(y-sy)*12+(m-sm);if(diff<0)return[];if(r.recurrence==='weekly'){const first=new Date(sy,sm-1,sd,12),ms=new Date(y,m-1,1,12),me=new Date(y,m-1,last,12);if(first>me)return[];let cur=new Date(first);if(cur<ms)cur=new Date(cur.getTime()+Math.ceil((ms-cur)/604800000)*604800000);const out=[];while(cur<=me){const ds=dateStr(cur.getFullYear(),cur.getMonth()+1,cur.getDate());if(ds>=start)out.push(ds);cur=new Date(cur.getTime()+604800000)}return out}if(r.recurrence==='yearly'&&diff%12!==0)return[];if(r.recurrence==='quarterly'&&diff%3!==0)return[];const ds=dateStr(y,m,due);return ds>=start?[ds]:[]}
async function recurringTotal(propertyId,y,m){const client=window.AP207Supabase;if(!client||!propertyId)return 0;try{const {data,error}=await client.from('recurring_expenses').select('*').eq('property_id',propertyId);if(error)return 0;const td=today();return(data||[]).reduce((sum,r)=>sum+occurrences(r,y,m).filter(d=>d<=td).length*(Number(r.amount)||0),0)}catch{return 0}}
async function calc(p,y,m){const x=baseCalc(p,y,m);x.recurring=await recurringTotal(p.id,y,m);x.payout=x.gross-x.cleaning-x.commission-x.expenses-x.recurring;return x}
function set(id,v){const e=document.getElementById(id);if(e)e.textContent=v}
function neg(v){return v?`− ${money.format(v)}`:money.format(0)}
async function syncMonthly(){const sel=document.getElementById('propertySelector'),rm=document.getElementById('reportMonth'),ry=document.getElementById('reportYear');if(!sel||!rm||!ry)return;const p=props().find(x=>x.id===sel.value);if(!p)return;const m=Number(rm.value)+1,y=Number(ry.value);if(!m||!y)return;const x=await calc(p,y,m),days=dim(y,m),occupied=Math.min(days,x.nights),avg=occupied?x.gross/occupied:0;set('reportGross',money.format(x.gross));set('reportAverageDailyRate',money.format(avg));set('reportCleaning',money.format(x.cleaning));set('reportCommission',money.format(x.commission));set('reportSummaryGross',money.format(x.gross));set('reportSummaryCleaning',neg(x.cleaning));set('reportSummaryCommission',neg(x.commission));set('reportBeforeExpenses',money.format(x.gross-x.cleaning-x.commission));set('reportOtherExpenses',neg(x.expenses));let row=document.getElementById('reportRecurringExpensesLine');const ordinary=document.getElementById('reportOtherExpenses')?.closest('.line');if(ordinary&&!row){row=document.createElement('div');row.id='reportRecurringExpensesLine';row.className='line subtraction';row.innerHTML='<dt>− Despesas recorrentes</dt><dd id="reportRecurringExpenses">R$ 0,00</dd>';ordinary.insertAdjacentElement('afterend',row)}set('reportRecurringExpenses',neg(x.recurring));set('reportFinalPayout',money.format(x.payout))}
function selectedConsolidatedProps(){const all=props(),scope=document.getElementById('consolidatedScope')?.value||'all',admin=document.getElementById('consolidatedAdmin')?.value||'all';let list=all;if(admin!=='all')list=list.filter(p=>String(p.administratorId||p.adminId||p.administratorName||p.adminName||'')===admin);if(scope.startsWith('property:'))list=list.filter(p=>p.id===scope.slice(9));return list}
async function syncConsolidated(){const m=Number(document.getElementById('consolidatedMonth')?.value||0),y=Number(document.getElementById('consolidatedYear')?.value||0);if(!m||!y)return;const list=selectedConsolidatedProps();const rows=await Promise.all(list.map(async p=>({p,...await calc(p,y,m)})));const t=rows.reduce((a,r)=>{for(const k of ['reservations','nights','gross','cleaning','commission','expenses','recurring','payout'])a[k]=(a[k]||0)+(Number(r[k])||0);return a},{});const capacity=dim(y,m)*rows.length,occ=capacity?(t.nights/capacity)*100:0,avg=t.nights?t.gross/t.nights:0;set('cProperties',String(rows.length));set('cReservations',String(t.reservations||0));set('cNights',String(t.nights||0));set('cAvailable',String(Math.max(0,capacity-(t.nights||0))));set('cOccupancy',`${occ.toLocaleString('pt-BR',{maximumFractionDigits:2})}%`);set('cGross',money.format(t.gross||0));set('cAverageDaily',money.format(avg));set('cCleaning',neg(t.cleaning||0));set('cCommission',neg(t.commission||0));set('cExpenses',neg(t.expenses||0));let rec=document.getElementById('cRecurringExpenses');if(!rec){const exp=document.getElementById('cExpenses')?.parentElement;if(exp){const d=document.createElement('div');d.innerHTML='<small>Despesas recorrentes</small><strong id="cRecurringExpenses">R$ 0,00</strong>';exp.insertAdjacentElement('afterend',d);rec=d.querySelector('strong')}}if(rec)rec.textContent=neg(t.recurring||0);set('cPayout',money.format(t.payout||0));
 const ownerTotals=document.getElementById('ownerTotals');if(ownerTotals){const groups=new Map();for(const r of rows){const key=r.p.ownerName||'Sem proprietário';if(!groups.has(key))groups.set(key,[]);groups.get(key).push(r)}for(const card of ownerTotals.querySelectorAll('.general-owner-card')){const name=card.querySelector('h3')?.textContent?.trim(),g=groups.get(name);if(!g)continue;const z=g.reduce((a,r)=>{for(const k of ['gross','cleaning','commission','expenses','recurring','payout'])a[k]=(a[k]||0)+(Number(r[k])||0);return a},{}),cells=[...card.querySelectorAll('.report-metrics>div')];const byLabel=l=>cells.find(c=>c.querySelector('small')?.textContent.trim()===l)?.querySelector('strong');const put=(l,v)=>{const e=byLabel(l);if(e)e.textContent=v};put('Valor bruto',money.format(z.gross||0));put('Limpeza',neg(z.cleaning||0));put('Comissão',neg(z.commission||0));put('Despesas',neg(z.expenses||0));let rr=byLabel('Despesas recorrentes');if(!rr){const metric=card.querySelector('.report-metrics');if(metric){const d=document.createElement('div');d.innerHTML='<small>Despesas recorrentes</small><strong></strong>';metric.append(d);rr=d.querySelector('strong')}}if(rr)rr.textContent=neg(z.recurring||0);put('Saldo líquido',money.format(z.payout||0))}}
 const detail=document.getElementById('consolidatedRows');if(detail){for(const card of detail.querySelectorAll('.consolidated-row')){const unit=card.querySelector('div:nth-child(2) strong')?.textContent?.trim(),r=rows.find(x=>(x.p.unit||x.p.name||'—')===unit);if(!r)continue;const cells=[...card.querySelectorAll('div')],byLabel=l=>cells.find(c=>c.querySelector('small')?.textContent.trim()===l)?.querySelector('strong'),put=(l,v)=>{const e=byLabel(l);if(e)e.textContent=v};put('Valor bruto',money.format(r.gross));put('Limpeza',neg(r.cleaning));put('Comissão',neg(r.commission));put('Despesas',neg(r.expenses));let rr=byLabel('Despesas recorrentes');if(!rr){const d=document.createElement('div');d.innerHTML='<small>Despesas recorrentes</small><strong></strong>';card.append(d);rr=d.querySelector('strong')}rr.textContent=neg(r.recurring);put('Saldo líquido',money.format(r.payout))}}}
function bind(){document.getElementById('generateReportButton')?.addEventListener('click',()=>setTimeout(syncMonthly,120));['reportMonth','reportYear','propertySelector'].forEach(id=>document.getElementById(id)?.addEventListener('change',()=>setTimeout(syncMonthly,120)));document.getElementById('refreshConsolidated')?.addEventListener('click',()=>setTimeout(syncConsolidated,120));setTimeout(()=>{syncMonthly();syncConsolidated()},500)}
window.StayFinancialEngine={calc,syncMonthly,syncConsolidated};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(bind,1200),{once:true});else setTimeout(bind,1200);})();