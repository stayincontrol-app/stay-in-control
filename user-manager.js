(()=>{'use strict';
const AUTH_KEY='ap207-auth-profile-v1',INVITES_KEY='stay-control-invitations-v1',PROPS_KEY='ap207-dashboard-properties-v1';
const REDIRECT_URL='https://ap207-dashboard.vercel.app/invite.html';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch{return f}};
const auth=()=>read(AUTH_KEY,null),role=()=>auth()?.profile?.role,saveInvites=x=>localStorage.setItem(INVITES_KEY,JSON.stringify(x));
const properties=()=>read(PROPS_KEY,{properties:[]}).properties||[];
function visibleProperties(){const a=auth(),ps=properties();if(a?.profile?.role==='super_admin')return ps;const allowed=new Set(a?.propertyIds||[]);return ps.filter(p=>allowed.has(p.id))}
function canManage(i){const a=auth();if(!a?.profile)return false;if(a.profile.role==='super_admin')return i.role!=='super_admin';return a.profile.role==='admin'&&i.role==='owner'&&i.createdBy===a.profile.id}
function styles(){if(document.getElementById('stayUserStyles'))return;const s=document.createElement('style');s.id='stayUserStyles';s.textContent='.stay-users{margin-top:18px}.stay-user-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:16px;padding-top:16px;border-top:1px solid #dde3ea}.stay-user-form .wide{grid-column:1/-1}.stay-user-list{display:grid;gap:10px;margin-top:14px}.stay-user-row{padding:14px;border:1px solid #e2e8f0;border-radius:14px;background:#fff;display:grid;gap:10px}.stay-user-row small{display:block;color:#64748b;line-height:1.45;margin-top:3px}.stay-user-row-actions{display:flex;gap:8px;flex-wrap:wrap}.stay-delete-invite,.stay-send-invite,.stay-resend-invite{border:1px solid transparent;border-radius:12px;padding:10px 14px;font-weight:800;min-height:44px}.stay-delete-invite{background:#dc2626;color:#fff}.stay-send-invite{background:#2563eb;color:#fff}.stay-resend-invite{background:#173a8f;color:#fff}.stay-status-sent{color:#166534!important;font-weight:700}.stay-checks{display:grid;gap:8px;padding:10px;border:1px solid #e2e8f0;border-radius:12px}.stay-checks label{display:flex;gap:8px;align-items:center}.stay-success{color:#166534;font-weight:800}.stay-error{color:#b42318;font-weight:800}.stay-loading{color:#64748b;font-weight:700}@media(max-width:700px){.stay-user-form{grid-template-columns:1fr}.stay-user-form .wide{grid-column:auto}.stay-delete-invite,.stay-send-invite,.stay-resend-invite{flex:1 1 125px}}';document.head.append(s)}
async function callFunction(name,body){if(!window.AP207Supabase)throw new Error('O serviço de acesso ainda está carregando.');const {data,error}=await window.AP207Supabase.functions.invoke(name,{body});if(error){let detail='';try{if(error.context&&typeof error.context.json==='function')detail=String((await error.context.json())?.error||'')}catch{}throw new Error(detail||error.message||'Falha no serviço.')}if(!data?.ok)throw new Error(String(data?.error||'Falha no serviço.'));return data}
async function invokeAccess(invite,button,isResend,msg,refresh){button.disabled=true;const old=button.textContent;button.textContent=isResend?'Reenviando…':'Enviando…';msg.className='wide stay-loading';msg.textContent=isResend?'Reenviando acesso…':'Enviando primeiro acesso…';try{const redirectTo=REDIRECT_URL+'?invite=1&v='+Date.now();await callFunction('invite-user',{name:invite.name,email:invite.email,role:invite.role,propertyIds:invite.propertyIds||[],redirectTo,resend:isResend});const all=read(INVITES_KEY,[]),idx=all.findIndex(x=>x.email===invite.email);const saved={...invite,status:isResend?'reset_sent':'sent',sendCount:(invite.sendCount||0)+1,sentAt:new Date().toISOString(),redirectTo};if(idx>=0)all[idx]=saved;else all.push(saved);saveInvites(all);msg.className='wide stay-success';msg.textContent=isResend?'✅ Acesso reenviado com sucesso para '+invite.email+'.':'✅ Primeiro acesso enviado com sucesso para '+invite.email+'.';await refresh()}catch(e){msg.className='wide stay-error';msg.textContent='❌ '+(e.message||'Não foi possível enviar o acesso.');button.disabled=false;button.textContent=old}}
function install(){if(!['super_admin','admin'].includes(role())||document.getElementById('stayUserSection'))return;styles();const anchor=document.getElementById('propertySettings');if(!anchor)return;const section=document.createElement('section');section.id='stayUserSection';section.className='panel stay-users';section.innerHTML='<div class="panel-heading"><div><p class="eyebrow">Usuários e acessos</p><h2>Administradores e proprietários</h2></div><button id="stayInviteUser" class="button button-primary" type="button">+ Convidar usuário</button></div><p>Convide usuários e escolha quais propriedades poderão acessar.</p><form id="stayInviteForm" class="stay-user-form" hidden><div class="field"><label>Nome</label><input id="stayInviteName" required></div><div class="field"><label>E-mail</label><input id="stayInviteEmail" type="email" required></div><div class="field"><label>Tipo de acesso</label><select id="stayInviteRole"><option value="owner">Proprietário</option>'+(role()==='super_admin'?'<option value="admin">Administrador/Gestor</option>':'')+'</select></div><div class="field wide"><label>Propriedades liberadas</label><div id="stayInviteProperties" class="stay-checks"></div></div><p id="stayInviteMessage" class="wide"></p><div class="form-actions wide"><button class="button button-primary" type="submit">Criar convite</button><button id="stayCancelInvite" class="button button-secondary" type="button">Cancelar</button></div></form><div id="stayInviteList" class="stay-user-list"></div>';anchor.insertAdjacentElement('afterend',section);
const form=section.querySelector('#stayInviteForm'),checks=section.querySelector('#stayInviteProperties'),msg=section.querySelector('#stayInviteMessage'),list=section.querySelector('#stayInviteList');let remoteUsers=[];
const renderChecks=()=>{checks.replaceChildren();visibleProperties().forEach(p=>{const l=document.createElement('label');l.innerHTML='<input type="checkbox" value="'+p.id+'"><span>'+(p.unit||p.name)+' — '+(p.name||'')+'</span>';checks.append(l)})};
const removeUser=async(i,button)=>{if(!canManage(i)||!confirm('Excluir '+i.name+' do Stay in Control?'))return;button.disabled=true;button.textContent='Excluindo…';try{await callFunction('delete-user',{email:i.email});saveInvites(read(INVITES_KEY,[]).filter(x=>x.email!==i.email));msg.className='wide stay-success';msg.textContent='✅ '+i.name+' foi excluído(a).';await refresh()}catch(e){button.disabled=false;button.textContent='Excluir';msg.className='wide stay-error';msg.textContent='❌ '+e.message}};
const renderList=()=>{list.replaceChildren();const a=auth(),allLocal=read(INVITES_KEY,[]),local=a?.profile?.role==='admin'?allLocal.filter(i=>i.createdBy===a.profile.id):allLocal;const allowedEmails=new Set(local.map(i=>String(i.email||'').toLowerCase()));const byEmail=new Map();remoteUsers.forEach(u=>{const email=String(u.email||'').toLowerCase();if(a?.profile?.role==='super_admin'||allowedEmails.has(email))byEmail.set(email,{...u,status:'registered',sendCount:1,server:true})});local.forEach(i=>{const email=String(i.email||'').toLowerCase(),old=byEmail.get(email);byEmail.set(email,old?{...i,...old,name:i.name||old.name,role:i.role||old.role,propertyIds:(i.propertyIds&&i.propertyIds.length)?i.propertyIds:old.propertyIds,status:old.status==='registered'?(i.status||'registered'):i.status}:{...i})});const items=[...byEmail.values()].filter(i=>i.role!=='super_admin');if(!items.length){list.innerHTML='<small>Nenhum acesso enviado por você ainda.</small>';return}items.sort((a,b)=>(a.name||a.email).localeCompare(b.name||b.email)).forEach(i=>{const d=document.createElement('div');d.className='stay-user-row';const registered=i.server||i.status==='registered'||i.status==='sent'||i.status==='reset_sent'||(i.sendCount||0)>0;const label=registered?'usuário cadastrado':'convite preparado';d.innerHTML='<div><strong>'+(i.name||'Sem nome')+' — '+(i.role==='admin'?'Administrador':'Proprietário')+'</strong><small class="'+(registered?'stay-status-sent':'')+'">'+i.email+' • '+((i.propertyIds||[]).length)+' propriedade(s) • '+label+'</small></div>';if(canManage(i)){const actions=document.createElement('div');actions.className='stay-user-row-actions';const access=document.createElement('button');access.type='button';access.className=registered?'stay-resend-invite':'stay-send-invite';access.textContent=registered?'Reenviar acesso':'Enviar acesso';access.onclick=()=>invokeAccess(i,access,registered,msg,refresh);actions.append(access);const del=document.createElement('button');del.type='button';del.className='stay-delete-invite';del.textContent='Excluir';del.onclick=()=>removeUser(i,del);actions.append(del);d.append(actions)}list.append(d)})};
const refresh=async()=>{list.innerHTML='<small>Carregando usuários…</small>';try{const data=await callFunction('list-users',{});remoteUsers=Array.isArray(data.users)?data.users:[];renderList()}catch(e){remoteUsers=[];renderList();msg.className='wide stay-error';msg.textContent='❌ Não foi possível recuperar os usuários do servidor: '+e.message}};
section.querySelector('#stayInviteUser').onclick=()=>{renderChecks();msg.textContent='';form.hidden=false};section.querySelector('#stayCancelInvite').onclick=()=>{form.reset();form.hidden=true};
form.onsubmit=async e=>{e.preventDefault();const a=auth(),name=section.querySelector('#stayInviteName').value.trim(),email=section.querySelector('#stayInviteEmail').value.trim().toLowerCase(),r=section.querySelector('#stayInviteRole').value,ids=[...checks.querySelectorAll('input:checked')].map(x=>x.value);if(!name||!email||(!ids.length&&r==='owner')){msg.className='wide stay-error';msg.textContent='Preencha os dados e escolha a propriedade.';return}const all=read(INVITES_KEY,[]),remote=remoteUsers.find(u=>u.email===email),existing=all.find(i=>i.email===email&&((a.profile.role==='super_admin')||i.createdBy===a.profile.id));if(remote){const item={id:remote.id,name,email,role:r,propertyIds:ids,createdBy:a.profile.id,status:'registered',sendCount:1,server:true};const idx=all.findIndex(i=>i.email===email&&((a.profile.role==='super_admin')||i.createdBy===a.profile.id));if(idx>=0)all[idx]=item;else all.push(item);saveInvites(all);msg.className='wide stay-success';msg.textContent='✅ Este usuário já possui cadastro. O botão agora está como “Reenviar acesso”.';renderList()}else if(existing){existing.name=name;existing.role=r;existing.propertyIds=ids;saveInvites(all);msg.className='wide stay-success';msg.textContent='Usuário já preparado. Use o botão de acesso abaixo.';renderList()}else{all.push({id:'invite-'+Date.now(),name,email,role:r,propertyIds:ids,createdBy:a.profile.id,status:'pending',sendCount:0,createdAt:new Date().toISOString()});saveInvites(all);msg.className='wide stay-success';msg.textContent='Convite preparado. Use “Enviar acesso”.';renderList()}};
refresh()}
function boot(){const t=setInterval(()=>{if(document.body.classList.contains('ap207-authenticated')){install();if(document.getElementById('stayUserSection'))clearInterval(t)}},250);setTimeout(()=>clearInterval(t),15000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();