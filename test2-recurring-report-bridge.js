(()=>{'use strict';const CACHE='stay-control-recurring-expenses-v1:',EXP='ap207-dashboard-expenses-v1';function read(k,f){try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch{return f}}function dateOnly(v){return /^\d{4}-\d{2}-\d{2}$/.test(String(v||''))?String(v):''}function daysInMonth(y,m){return new Date(y,m,0).getDate()}function occurrenceDates(r,y,m){if(!r?.active)return[];const start=dateOnly(r.starts_on);if(!start)return[];const startD=new Date(start+'T00:00:00Z'),monthStart=new Date(Date.UTC(y,m-1,1)),monthEnd=new Date(Date.UTC(y,m,1));if(startD>=monthEnd)return[];const due=Math.min(daysInMonth(y,m),Math.max(1,Number(r.due_day)||new Date(startD).getUTCDate()));if(r.recurrence==='monthly'){const d=`${y}-${String(m).padStart(2,'0')}-${String(due).padStart(2,'0')}`;return d>=start?[d]:[]}if(r.recurrence==='quarterly'||r.recurrence==='yearly'){const sm=startD.getUTCMonth()+1,sy=startD.getUTCFullYear(),delta=(y-sy)*12+(m-sm),step=r.recurrence==='quarterly'?3:12;if(delta<0||delta%step)return[];return[`${y}-${String(m).padStart(2,'0')}-${String(due).padStart(2,'0')}`]}if(r.recurrence==='weekly'){const out=[],d=new Date(Math.max(startD.getTime(),monthStart.getTime()));const weekday=startD.getUTCDay();while(d.getUTCDay()!==weekday)d.setUTCDate(d.getUTCDate()+1);while(d<monthEnd){out.push(d.toISOString().slice(0,10));d.setUTCDate(d.getUTCDate()+7)}return out}return[]}function norm(s){return String(s||'').trim().toLowerCase().replace(/\s+/g,' ')}function existing(propertyId){const s=read(`${EXP}:${propertyId}`,{expenses:[]});return Array.isArray(s.expenses)?s.expenses:[]}function dueForMonth(propertyId,y,m){const c=read(CACHE+propertyId,{items:[]}),rows=Array.isArray(c.items)?c.items:[],made=existing(propertyId),today=new Date(),todayKey=`${today.getFullYear()}-${String(today.getMonth()+1).padStart(2,'0')}-${String(today.getDate()).padStart(2,'0')}`,out=[];rows.forEach(r=>occurrenceDates(r,y,m).forEach(date=>{if(date>todayKey)return;const amount=Math.max(0,Number(r.amount)||0);if(!amount)return;const duplicate=made.some(e=>String(e.date||'')===date&&Math.abs((Number(e.value)||0)-amount)<0.005&&norm(e.category)===norm(r.category)&&norm(e.description)===norm(r.description));if(!duplicate)out.push({date,category:r.category||'Outro',description:r.description||'Despesa recorrente',value:amount,recurring:true,recurringId:r.id})}));return out}window.StayControlRecurringReports={dueForMonth};window.dispatchEvent(new Event('stay:recurring-report-bridge-ready'))})();