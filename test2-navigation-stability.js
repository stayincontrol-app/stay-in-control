(()=>{'use strict';const MAP={home:'home',reservations:'reservations',calendar:'calendar',expenses:'expenses',reports:'reports'},EXTRA={operations:'Operations',contracts:'Contracts',integrations:'Integrations',banners:'Banners',analytics:'Analytics',audit:'Audit'};function baseButtons(){return[...document.querySelectorAll('.app-nav [data-screen]')]}function basePanels(){return[...document.querySelectorAll('.app-screen[data-screen-panel]')]}function extraButtons(){return[...document.querySelectorAll('#t2ExtraNav [data-t2]')]}function suite(){return document.getElementById('t2Suite')}function showBase(name,button){const target=MAP[name]||'home';const panels=basePanels();if(!panels.length)return false;panels.forEach(p=>{p.hidden=p.dataset.screenPanel!==target});const root=suite();if(root)root.hidden=true;baseButtons().forEach(b=>b.classList.toggle('active',b===button||b.dataset.screen===target));extraButtons().forEach(b=>b.classList.remove('active'));document.body.dataset.t2CurrentScreen=target;return true}function showExtra(name,button){const root=suite();if(!root)return false;basePanels().forEach(p=>p.hidden=true);root.hidden=false;Object.entries(EXTRA).forEach(([key,id])=>{const panel=document.getElementById('t2'+id);if(panel)panel.classList.toggle('t2-hidden',key!==name)});baseButtons().forEach(b=>b.classList.remove('active'));extraButtons().forEach(b=>b.classList.toggle('active',b===button||b.dataset.t2===name));document.body.dataset.t2CurrentScreen='extra:'+name;return true}function repair(){if(!document.body.classList.contains('ap207-authenticated'))return;const panels=basePanels(),root=suite(),anyBase=panels.some(p=>!p.hidden),anySuite=root&&!root.hidden&&!root.querySelectorAll('.t2-hidden').length;if(anyBase||root&&!root.hidden)return;const active=baseButtons().find(b=>b.classList.contains('active'));showBase(active?.dataset.screen||document.body.dataset.t2CurrentScreen||'home',active)}function install(){document.addEventListener('click',e=>{const b=e.target.closest('.app-nav [data-screen]');if(b){e.preventDefault();showBase(b.dataset.screen,b);requestAnimationFrame(()=>window.scrollTo({top:0,behavior:'auto'}));return}const x=e.target.closest('#t2ExtraNav [data-t2]');if(x){e.preventDefault();showExtra(x.dataset.t2,x);requestAnimationFrame(()=>document.getElementById('t2Suite')?.scrollIntoView({block:'start',behavior:'auto'}))}},true);const mo=new MutationObserver(()=>requestAnimationFrame(repair));mo.observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['hidden','class']});window.addEventListener('pageshow',()=>setTimeout(repair,50),{passive:true});window.addEventListener('load',()=>setTimeout(repair,100),{once:true});setTimeout(repair,350);setTimeout(repair,1200)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install()})();